Basically the code is all here what you need to do is
>Git credential store
so that you don't have to manually type the password then
>cp .sh to your bin
>make it executable
>run it on a cron job
and just double check all the directory locations are correct. Test it but it should just work
